"use client";
import Link from "next/link";
import AuthForm from "./AuthForm";

const LoginForm: React.FC = () => {
  return (
    <div className="container flex h-screen w-screen flex-col items-center justify-center"></div>
  );
};

export default LoginForm;
